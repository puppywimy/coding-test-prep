https://www.acmicpc.net/problem/1991

### 풀이 날짜
2025-09-12

### 문제 분석 요약
재귀를 이용하여 풀이, 전위/중위/후위 순회 문제

### 알고리즘 설계
조건
- 1 <= N <= 26

1. **트리 정리**

   입력값을 'tree' dictionary에 정리한다.
   ```py
   # 구조
   tree = {
       'A': {
           'left': 'B',
           'right': 'C'
       },
       'B': {
           'left': 'D',
           'right': '.'
       },
       # ...
   }
   ```
2. **함수 정의**
   ```py
   def preorderTraversal(node): # ...
   def inorderTraversal(node): # ...
   def postorderTraversal(node): # ...
   ```
3. **Base Condition**

   현재 노드가 '.' 즉 비어있으면 '' 반환
4. **재귀 식**
   ```py
   def preorderTraversal(node):
       # 1. 현재 노드 반환
       # 2. preorderTraversal(tree[node]['left'])
       # 3. preorderTraversal(tree[node]['right'])
   def inorderTraversal(node):
       # 1. inorderTraversal(tree[node]['left'])
       # 2. 현재 노드 반환
       # 3. inorderTraversal(tree[node]['right'])
   def postorderTraversal(node):
       # 1. postorderTraversal(tree[node]['left'])
       # 2. postorderTraversal(tree[node]['right'])
       # 3. 현재 노드 반환
   ```

### 코드
```js
# 제출 시 파일 입출력 부분 지우기

import sys

sys.stdin = open("input", "r")


def solution():
    N = int(input())
    tree = {}
    for _ in range(N):
        parent, left, right = input().split(" ")
        tree[parent] = {"left": left, "right": right}

    def preorderTraversal(node):
        if node == ".":
            return ""
        result = ""
        result += node
        result += preorderTraversal(tree[node]["left"])
        result += preorderTraversal(tree[node]["right"])
        return result

    def inorderTraversal(node):
        if node == ".":
            return ""
        result = ""
        result += inorderTraversal(tree[node]["left"])
        result += node
        result += inorderTraversal(tree[node]["right"])
        return result

    def postorderTraversal(node):
        if node == ".":
            return ""
        result = ""
        result += postorderTraversal(tree[node]["left"])
        result += postorderTraversal(tree[node]["right"])
        result += node
        return result

    output = [preorderTraversal("A"), inorderTraversal("A"), postorderTraversal("A")]
    print("\n".join(output))


solution()
```

### 시간 복잡도
세 함수 모두 O(N), 각 노드에 한 번씩 방문하기 때문

### 느낀점 or 기억할 정보
- 이 부분은 학교에서 배웠던 내용이라 빠르게 풀 수 있었다.

https://www.acmicpc.net/problem/1912

### 풀이 날짜
2025-09-08

### 문제 분석 요약
- 최대 구간 합 문제: 연속된 부분 배열의 합의 최댓값을 구하는 문제
### 알고리즘 설계
#### 필요한 값
- numbers 배열
#### 풀이순서
1. 누적합 테이블을 먼저 구함.
2. 누적합 테이블을 순회하며 구간 합의 최댓값을 구함.

   - minPrefixSum: 지금까지의 누적합의 최솟값
   
   - max: <i>max</i>(max, 현재값 - 지금까지의 누적합의 최솟값)

### 코드
```js
const fs = require("fs");
const rawInput = fs.readFileSync("input").toString().trim(); // fs.readFileSync(0, "utf-8").trim();

const numbers = rawInput.split("\n")[1].split(" ").map(Number);
const prefixSumTable = [numbers[0]];
for (let i = 1; i < numbers.length; i++) {
  prefixSumTable[i] = prefixSumTable[i - 1] + numbers[i];
}
let minPrefixSum = 0;
let max = prefixSumTable[0];
for (let i = 0; i < numbers.length; i++) {
  const candidate = prefixSumTable[i] - minPrefixSum;
  if (candidate > max) max = candidate;
  if (prefixSumTable[i] < minPrefixSum) {
    minPrefixSum = prefixSumTable[i];
  }
}

console.log(max);
```

### 시간 복잡도
- O(N)
    - 누적합(O(N)), 최댓값(O(N)), 따라서 O(N)

### 느낀점 or 기억할 정보
구간 합을 구해야한다는 것을 보고 누적합을 떠올림 -> 누적합 테이블에서 최대값 - 최소값을 찾아야한다고 생각 -> ??

여기서 GPT의 도움을 빌려 (현재값 - 현재까지의 최솟값)이 최대가 되는 값이 정답임을 확인

(카데인 알고리즘을 이용하면 쉽게 풀 수 있음!)

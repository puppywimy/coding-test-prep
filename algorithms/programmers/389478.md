### 문제 및 풀이 날짜

- https://school.programmers.co.kr/learn/courses/30/lessons/389478
- 2025-11-10

### 문제 분석 요약

구현 문제? 수학 문제?

### 제한 사항

- 2 ≤ `n` ≤ 100
- 1 ≤ `w` ≤ 10
- 1 ≤ `num` ≤ `n`

### 알고리즘 설계

어떤 알고리즘으로 풀어야 할지 찾지 못하고 있었는데, n의 크기가 작은 것을 보고 그대로 구현해서 풀 수도 있겠다고 생각했다.

1. 규칙에 따라 w로 나누어 떨어지는 index마다 방향을 바꾸어 숫자를 저장<br />
   이때 num의 좌표를 저장
2. 저장된 num의 좌표를 이용하여 num 위에 쌓인 상자 개수를 세어 반환

### 코드

```js
function solution(n, w, num) {
  const height = Math.ceil(n / w);
  const storage = Array.from({ length: height }).map(() => []);
  let index = 0;
  let [targetX, targetY] = [null, null];
  while (index < n) {
    const [x, y] = [
      Math.floor(index / w),
      Math.floor(index / w) % 2 === 0 ? index % w : w - (index % w) - 1,
    ];
    storage[x][y] = ++index;
    if (index === num) [targetX, targetY] = [x, y];
  }
  let count = 0;
  for (let i = targetX; i < height; i++) {
    if (storage[i][targetY] !== undefined) count++;
  }
  return count;
}
```

### 시간 복잡도

**O(n)**<br />
1부터 n까지의 숫자를 모두 저장하기 때문

### 느낀점 or 기억할 정보

- 수식을 만들어 O(1)의 시간 복잡도로 풀 수 있을 것 같기도 하다..!

https://www.acmicpc.net/problem/15650

### 풀이 날짜

2025-10-13

### 문제 분석 요약

백트래킹을 이용한 조합 구하기

### 알고리즘 설계

조건

- 1 <= M <= N <= 8

시간 복잡도를 계산해보면

1. 조합의 최대 가짓수: 8C4 = 70
2. 출력: isUsed를 순회해야 하므로 N, 따라서 8

70 \* 8 = 56<br />
안전한 풀이

1. 사전 준비

   ```py
   isUsed = [False, False, False]

   # count: 지금까지 선택한 개수, lastNumber: 마지막으로 선택한 가장 큰 수
   def recursive(count, lastNumber):
   ```

2. Base Condition

   count가 M이 되면 완료<br />
   따라서 이때의 결과를 출력

3. 재귀 함수

   ```py
   def recursive(count, lastNumber):
     # Base Condition
     if count == M:
       # 현재 조합 출력

     restCount # 현재 선택하는 것 제외, 남은 선택 개수
     for number in range(lastNumber + 1, N + 1 - restCount):
       isUsed[number] = True
       recursive(count + 1, number)
       isUsed[number] = False
   ```

### 코드

```py
def solution():
    N, M = map(int, input().split(" "))
    isUsed = [False for _ in range(N + 1)]  # 1-based indexing

    def recursive(count, lastNumber):
        if count == M:
            selections = []
            for i in range(N + 1):
                if isUsed[i]:
                    selections.append(i)
            print(" ".join(map(str, selections)))
            return

        restCount = M - 1 - count
        for number in range(lastNumber + 1, N + 1 - restCount):
            isUsed[number] = True
            recursive(count + 1, number)
            isUsed[number] = False

    recursive(0, 0)


solution()
```

### 시간 복잡도

알고리즘 설계 참고.

### 느낀점 or 기억할 정보

조합을 구하는 방법은 다른 문제에서도 자주 출제되는 것 같다.<br />
내 것으로 만들어 바로바로 사용할 수 있도록 하자.

### 문제 및 풀이 날짜

- https://www.acmicpc.net/problem/1149
- 2025-10-30

### 문제 분석 요약

2차원 DP 문제

### 제한사항

- 2 ≤ N ≤ 1,000
- 1 ≤ 집을 칠하는 비용 ≤ 1,000

### 알고리즘 설계

규칙을 읽어보면 인접한 집은 같은 색이어서는 안 된다는 얘기이다.<br />
모든 집을 칠하는 비용의 최솟값을 구하는 문제이고, 모든 경우의 수를 구할 수는 없으니(3^1,000) DP로 풀어야한다는 확신이 생겼다.

1. 테이블 정의

   ```
   i: 0 ~ N-1
   c: 0 - R, 1 - G, 2 - B

   D[i][c] = i번째 집을 c로 칠할 때 지금까지의 비용의 최솟값
   ```

2. 점화식 찾기
   ```
   D[i][0] = min(D[i - 1][1], D[i - 1][2]) + prices[i][0]
   D[i][1] = min(D[i - 1][0], D[i - 1][2]) + prices[i][1]
   D[i][2] = min(D[i - 1][0], D[i - 1][1]) + prices[i][2]
   ```
3. 초기값 정하기

   D[0]은 점화식으로 구할 수 없으므로

   ```
   D[0] = prices[0]
   ```

### 코드

```py
def solution():
    N = int(input())
    prices = []
    for _ in range(N):
        prices.append(list(map(int, input().split(" "))))
    table = [[None, None, None] for _ in range(N)]
    table[0] = prices[0]
    for i in range(1, N):
        table[i][0] = min(table[i - 1][1], table[i - 1][2]) + prices[i][0]
        table[i][1] = min(table[i - 1][0], table[i - 1][2]) + prices[i][1]
        table[i][2] = min(table[i - 1][0], table[i - 1][1]) + prices[i][2]
    print(min(table[N - 1][0], table[N - 1][1], table[N - 1][2]))


solution()
```

### 시간 복잡도

**O(N)**<br />
0 ~ N-1까지 N번 연산을 수행하기 때문

<!-- ### 느낀점 or 기억할 정보 -->

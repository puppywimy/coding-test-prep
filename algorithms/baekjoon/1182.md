https://www.acmicpc.net/problem/1182

### 풀이 날짜
2025-09-17

### 문제 분석 요약
모든 부분수열을 구하는 문제

### 알고리즘 설계
조건
- 1 <= N <= 20, |S| <= 1,000,000, |a| < 100,000

부분수열의 수는 2^{원소 수}이므로 전수조사로 풀 수 있음.

또한 모든 원소 합의 절댓값은 2,000,000을 넘지 않음.

1. **함수 정의**
   ```py
   numbers # 원소 배열
   count # 경우의 수
   
   def recursive(현재 원소 인덱스, 현재까지의 합): # ...
   ```
2. **Base Condition**

   현재 원소 인덱스가 N과 같다면 마지막 원소까지 탐색을 완료한 경우

   이때 현재까지의 합이 S와 같다면 count 1 증가
4. **재귀 식**
   ```py
   def recursive(index, total):
       Base Condition

       recursive(index + 1, total)
       recursive(index + 1, total + numbers[index])
   ```
5. **공집합에 대한 처리**

   S = 0인 경우 부분수열이 공집합인 경우를 포함하므로 count에서 1을 빼줘야한다.


### 코드
```py
# 제출 시 파일 입출력 부분 지우기

import sys

sys.stdin = open("input", "r")


def solution():
    N, S = map(int, input().split(" "))
    numbers = list(map(int, input().split(" ")))
    count = 0

    def recursive(index, total):
        nonlocal count

        if index == N:
            if total == S:
                count += 1
            return

        recursive(index + 1, total)
        recursive(index + 1, total + numbers[index])

    recursive(0, 0)
    if S == 0:
        count -= 1
    print(count)


solution()
```

### 시간 복잡도
O(2^N), 재귀함수를 2번씩 호출하며 모든 경우를 탐색하기 때문

### 느낀점 or 기억할 정보
백트래킹 문제들을 풀면서 느낀점인데, 상태공간트리를 직접 그려보면 재귀 식을 세우거나 시간 복잡도를 구할 때 도움이 되는 것 같다.
